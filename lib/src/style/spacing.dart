import 'package:flutter/material.dart';

enum Spacing { xsmall, small, medium, large, xlarge, xxlarge }

class Space extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
