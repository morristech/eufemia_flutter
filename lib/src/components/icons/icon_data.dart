import 'package:flutter/widgets.dart';

/// {@category Components}
/// {@subCategory Icons}
/// Static access class for [EufemiaIcons] data.
class EufemiaIconData extends IconData {
  const EufemiaIconData(int codePoint)
      : super(
          codePoint,
          fontFamily: 'EufemiaIcons',
          fontPackage: 'eufemia',
        );
}
